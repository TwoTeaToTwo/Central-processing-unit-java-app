<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.twoteatotwo.lesson02">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_cpu_r"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_cpu_r_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lesson02"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Lesson02.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Text_Content_Activity" />
        <activity android:name=".Compare_Content_Activity" />
        <activity android:name=".Web_Content_Activity" />
    </application>

</manifest>